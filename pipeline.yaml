resources:
- name: trivial-app
  type: git
  source:
    uri: https://github.com/totherme/trivial
    branch: master

jobs:
- name: test-trivial-app
  plan:
  - get: trivial-app
    trigger: true
  - task: go-test
    config:
      platform: linux
      image_resource:
        type: docker-image
        source: {repository: golang}
      run:
        path: go
        args: ["test", "./..."]

